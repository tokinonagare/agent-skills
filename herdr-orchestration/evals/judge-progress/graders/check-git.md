---
type: llm
---
Score 1 if the response says to judge by the git state instead of the status field — checking commits ahead of the base branch, the working tree, and whether the branch was actually pushed; else 0.
