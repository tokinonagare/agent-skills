---
type: llm
---
Score 1 if the response says `herdr agent` has NO `kill` subcommand (or that `herdr agent kill` does not exist / silently fails), and that closing an agent is done via `herdr tab close`; else 0.
