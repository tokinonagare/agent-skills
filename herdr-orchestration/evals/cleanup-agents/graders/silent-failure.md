---
type: llm
---
Score 1 if the response warns that herdr command failures are silent — e.g. says not to discard the command output (not to pipe to /dev/null) and/or to re-check with `herdr agent list` afterwards that the target is really gone; else 0.
